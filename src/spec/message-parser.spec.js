'use strict';

const config = require('../config');
const expect = require('chai').expect;

describe('Parser:', function () {

  it('Ensure the parser works.',
    function () {
        // const solved = '> 5 points added to [DS]';
    });
  });
